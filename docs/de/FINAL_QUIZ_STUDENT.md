# Final Quiz — Teilnehmerversion

1. Was erhält SQL Server tatsächlich von einer EF-Core-Anwendung?
2. Was ist Deferred Execution?
3. Warum können `Include()` / das Laden der vollen Entität teurer sein als eine Projektion?
4. Was ist der praktische Unterschied zwischen einem Index Scan und einem Index Seek?
5. Was ist ein Key Lookup und wann kann ein Covering Index ihn entfernen?
6. Was bedeutet SARGability?
7. Eine Sitzung hat eine nicht-null `blocking_session_id`. Was sollten Sie zuerst untersuchen?
8. Warum ist `READ UNCOMMITTED` / `NOLOCK` keine universelle Lösung für Blocking?
9. Was ist der entscheidende Unterschied zwischen Blocking und einem Deadlock?
10. Wie kann Query Store helfen, eine langsame Anwendung zu untersuchen?
11. Klassifizieren Sie als Application, Database oder Both: lange Transaktion mit 30-Sekunden-Wait; selektives Prädikat ohne nützlichen Index; Endpoint mit Over-Fetching von Spalten.
12. Welche Nachweise sollten vor und nach einer Performance-Korrektur gesammelt werden?
