# Final Quiz — Student Version

1. What does SQL Server actually receive from an EF Core application?
2. What is deferred execution?
3. Why can `Include()` / full entity loading be more expensive than projection?
4. What is the practical difference between an Index Scan and an Index Seek?
5. What is a Key Lookup and when can a covering index remove it?
6. What does SARGability mean?
7. A session has a non-zero `blocking_session_id`. What should you investigate first?
8. Why is `READ UNCOMMITTED` / `NOLOCK` not a universal blocking solution?
9. What is the key difference between blocking and a deadlock?
10. How can Query Store help investigate a slow application?
11. Classify as Application, Database or Both: long transaction with 30-second wait; selective predicate without useful index; endpoint over-fetching columns.
12. What evidence should be collected before and after a performance fix?
